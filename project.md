# Анализ кодовой базы: botForEKOAmulet

## 📁 Структура проекта
Проект имеет плоскую структуру с выделением вспомогательных директорий.

```
botForEKOAmulet/
├── bot.py                  # Основной файл приложения (Entry Point)
├── sheets_handler.py       # Слой работы с данными (Google Sheets)
├── requirements.txt        # Зависимости проекта
├── .env                    # Конфигурация и секреты
├── creds.json              # Ключи доступа к Google API
├── pending_payments.json   # Локальное хранилище незавершенных транзакций
├── tests/                  # Тесты
│   └── test_certificates.py # Unit-тесты для сертификатов
├── static/                 # Статические файлы (изображения и пр.)
├── scripts/                # Вспомогательные скрипты
└── logs/ (implied)         # Файлы логов (bot.log*)
```

**Принципы организации:**
- **Monolithic Script**: Основная логика сосредоточена в `bot.py`.
- **Service Layer**: Работа с Google Sheets вынесена в отдельный модуль `sheets_handler.py`.
- **Configuration**: Строгое отделение конфигурации от кода через `.env`.

## 🛠 Технологический стек

| Технология | Версия | Назначение |
|------------|--------|------------|
| **Python** | 3.x | Основной язык разработки |
| **python-telegram-bot** | 20.6 | Асинхронный фреймворк для Telegram ботов |
| **gspread** | 5.12.0 | API клиент для Google Sheets |
| **yookassa** | 3.3.0 | Платежная система |
| **aiohttp** | 3.9.1 | Асинхронные HTTP запросы / Webhook сервер |
| **pytest** | - | Фреймворк для тестирования |

## 🏗 Архитектура

Проект построен как асинхронное приложение на базе `asyncio`.

### 1. Управление состоянием (State Management)
Используется `ConversationHandler` из библиотеки `python-telegram-bot`. Это классический конечный автомат (FSM), где пользователь перемещается между стадиями:
- `ASKING_PHONE`
- `ASKING_ADDR`
- `ASKING_PAYMENT`
и т.д.

### 2. Слой данных (Data Access Layer)
Реализован в `sheets_handler.py`.
- **Паттерн**: Repository (упрощенный).
- **Особенности**: 
  - Изоляция логики gspread.
  - Автоматическое переподключение (Reconnect).
  - Поиск свободной строки для записи.

### 3. Обработка платежей
Интеграция с ЮКассой через Webhook.
- Создание платежа -> Redirect пользователя.
- Получение Webhook -> Проверка подписи (`hmac`) -> Обновление статуса.

### 4. Отказоустойчивость (Resilience)
- **Retry Logic**: Декораторы или циклы с экспоненциальной задержкой (`RETRY_BACKOFF`) для внешних API (Google Sheets).
- **Fallback**: Если Google Sheets недоступны, данные сохраняются локально в память/файл (`ORDERS_DATA`, `pending_payments.json`).
- **Concurrency**: Использование `asyncio.Lock` (`stock_lock`) для предотвращения Race Condition при изменении остатков товара.

## 🎨 UX и Взаимодействие (Bot UI)

### Интерфейс
- **Inline Keyboards**: Основной способ навигации (Кнопки "Купить", "Подробнее").
- **Deeplinks**: Поддержка ссылок вида `t.me/bot?start=help_project` для разных точек входа.

### Флоу пользователя
1. **Приветствие**: Sticker + Описание + Кнопка действия.
2. **Сбор данных**: Пошаговый опрос (ФИО, Телефон, Адрес). Валидация на каждом шаге (RegEx).
3. **Оплата**: Генерация ссылки на оплату.
4. **Фидбек**: Мгновенное подтверждение после оплаты и отправка инструкций.

## ✅ Качество кода

### Стандарты и чистота
- **Типизация**: Активно используется `typing` (Hinting), что облегчает поддержку.
- **Логирование**: Настроена ротация логов (`TimedRotatingFileHandler`). Логируются уровни INFO и ERROR. Есть фильтрация "шумных" логов `aiohttp`.
- **Безопасность**: Секреты не сохранены в коде. Проверка подписей вебхуков.

### Тестирование
- Найдены тесты в `tests/test_certificates.py`.
- Используется `unittest.mock` для моков внешних сервисов (Telegram API, Google Sheets).
- **Покрытие**: Покрыт функционал формирования сообщений о сертификатах. Основное ядро `bot.py` (FSM) тестами покрыто слабо (судя по файлам).

## 🔧 Ключевые компоненты

### 1. Менеджер Google таблиц (`sheets_handler.py`)
Обеспечивает надежную запись заказов.
```python
def add_order(self, ...):
    # Поиск следующей свободной строки и запись
    next_row = len(worksheet.col_values(1)) + 1
    target_range = f"A{next_row}:I{next_row}"
    worksheet.update(target_range, [row])
```

### 2. Безопасное обновление стока (`bot.py`)
Гарантирует, что товар не уйдет в минус при одновременных заказах.
```python
async def decrease_stock_safe() -> Optional[int]:
    async with stock_lock:  # Атомарная операция
        current = await _get_stock_no_lock()
        if current <= 0: return None
        # ... logic ...
```

### 3. Retry Mechanism (`bot.py`)
Не позволяет боту упасть при моргании сети.
```python
for attempt in range(MAX_RETRIES):
    try:
        # operation
    except Exception:
        delay = RETRY_DELAY * (RETRY_BACKOFF ** attempt)
        await asyncio.sleep(delay)
```

## 📋 Выводы и рекомендации

**Сильные стороны:**
1. **Prod-Ready**: Реализованы механизмы Retry, Logging, Graceful Degradation.
2. **Безопасность**: Корректная работа с секретами и Locks.
3. **Читаемость**: Код хорошо структурирован, функции имеют понятные названия и тайп-хинты.

**Зоны роста:**
1. **Монолитность `bot.py`**: Файл >2000 строк. Рекомендуется разделить на модули: `handlers.py`, `services.py`, `states.py`.
2. **Hardcoded Text**: Тексты сообщений зашиты в коде. Стоит вынести их в JSON/YAML файл локализации или констант.
3. **Configuration**: Цены и названия товаров частично в `.env`, частично в коде. Лучше вынести конфигурацию продуктов в отдельный конфиг-файл.
4. **Тесты**: Расширить покрытие тестами, особенно для FSM логики (ConversationHandler).

**Уровень проекта**: **Strong Middle**. Код демонстрирует понимание проблем продакшена (конкурентность, сбои сети), но архитектурно требует рефакторинга для дальнейшего масштабирования.
