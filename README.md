# ЭКОамулет Telegram Bot v4.0

Telegram бот для продажи "ЭКОамулет" — универсального термокомпозита. Бот поддерживает прием заказов, интеграцию с Google Sheets для учета остатков и заказов, а также обработку платежей через ЮКассу.

## Особенности

*   🛍️ **E-commerce**: Карточка товара, оформление заказа, сбор данных доставки.
*   💳 **Платежи**: Интеграция с ЮКассой (Webhook).
*   📊 **Учет**: Синхронизация остатков и заказов с Google Sheets.
*   🛡️ **Надежность**: Retry logic для внешних API, атомарные операции с остатками, резервное локальное хранилище.
*   📢 **Уведомления**: Оповещения администратора о новых заказах и критических ошибках.
*   ⏳ **Лист ожидания**: Возможность подписки на уведомления о поступлении товара.

## Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <your-repo-url>
    cd botForEKOAmulet
    ```

2.  **Создайте виртуальное окружение (рекомендуется):**
    ```bash
    python3 -m venv venv
    source venv/bin/activate  # Linux/macOS
    # venv\Scripts\activate  # Windows
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

## Настройка

1.  Переименуйте файл `.env` (если он есть как шаблон) или создайте новый на основе примера:
    ```bash
    cp .env .env.local
    ```
    *(Примечание: в репозитории может быть только шаблон, убедитесь, что создали файл с реальными данными)*

2.  **Заполните `.env` следующими данными:**

    ```env
    TELEGRAM_BOT_TOKEN=ваш_токен_бота
    ADMIN_TELEGRAM_ID=ваш_id_telegram
    ADMIN_CHAT_ID=id_чата_для_уведомлений
    
    # Настройки товара
    PRODUCT_NAME=ЭКОамулет
    PRODUCT_PRICE=1000
    
    # ЮКасса
    YOOKASSA_API_KEY=ваш_api_key
    YOOKASSA_SHOP_ID=ваш_shop_id
    
    # Google Sheets
    GOOGLE_SHEET_ID=id_вашей_таблицы
    
    # Webhook
    WEBHOOK_URL=https://ваш-домен.com
    WEBHOOK_SECRET=секретный_ключ
    ```

3.  **Google Sheets API**:
    Для работы с таблицами необходимо реализовать методы в `sheets_handler.py`. Сейчас там находятся заглушки. Вам потребуется настроить сервисный аккаунт Google и добавить логику работы с `gspread` или `google-api-python-client`.

## Запуск

```bash
python3 bot.py
```

## Структура проекта

*   `bot.py` — Основной файл запуска и логики бота.
*   `sheets_handler.py` — Модуль для работы с Google Sheets (требует реализации).
*   `requirements.txt` — Список зависимостей.
*   `.env` — Конфигурационные переменные (не добавляйте в git!).

## Разработка

*   Бот использует библиотеку `python-telegram-bot` (v20+).
*   Для обработки вебхуков используется `aiohttp`.
